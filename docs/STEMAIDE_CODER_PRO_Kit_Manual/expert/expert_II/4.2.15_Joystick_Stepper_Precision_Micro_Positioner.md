# 4.2.15. Joystick Stepper Precision Micro Positioner

| **Description** In Project 105, learners will construct an expert interactive system titled 'Joystick Stepper Precision Micro-Positioner'. This manual details the complete synthesis of XY Joystick + Stepper Motor + IIC 1602 LCD into an intuitive, high-performance automated control platform.|------------------|----------------------------------------------------------------|
| **Use case**     |This project connects to real-world industrial and IoT applications such as: Project 105 equips students with professional skills in embedded human-machine interface (HMI) design and automated motion control.|

## Components (Things You will need)

| ![ESP32 board](../../../assets/components/esp32.PNG) | ![Micro USB cable](../../../assets/components/usbcable.webp) | ![Bread Board](../../../assets/components/breadboard.webp) | ![Jumper Wires](../../../assets/components/jumperwires.webp) | ![LCD Display](../../../assets/components/lcdscreen.jpg) | ![Stepper Motor](../../../assets/components/steppermotor.webp) | ![Motor Driver](../../../assets/components/motordriver.jpg) | ![Joystick](../../../assets/components/joystick.jpg) |
| --- | --- | --- | --- | --- | --- | --- | --- |

## Building the circuit

> **Tip:** Use the [ESP32 Pin Mapping](../../assets/esp32_pin_mapping.png) image to locate the correct GPIO pins on your ESP32 board.


Things Needed:

- ESP32 board Core Board

- XY Joystick

- Stepper Motor

- IIC 1602 LCD

- USB Cable & Jumper Wires

- Bread Board

- Motor Driver


## Mounting the component on the breadboard and wiring the circuit

**Step 1:** Connect the ESP32 board 5V pin to the breadboard's positive (+) power rail and the ESP32 board GND pin to the negative (-) power rail.

**Step 2:** Place the XY Joystick on the breadboard.

  - Connect the VCC pin to the 5V power rail on the breadboard
  - Connect the GND pin to the ground rail on the breadboard
  - Connect the VRx pin to Analog GPIO 36
  - Connect the VRy pin to Analog GPIO 39
  - Connect the SW (switch) pin to Digital GPIO 2

**Step 3:** Place the ULN2003 Motor Driver Board on the breadboard (for the Stepper Motor).

  - Connect the VCC pin to the 5V power rail on the breadboard
  - Connect the GND pin to the ground rail on the breadboard
  - Connect IN1, IN2, IN3, and IN4 to Digital Pins 8, 9, 10, and 11 respectively
  - Plug the Stepper Motor connector into the driver board

**Step 4:** Place the IIC 1602 LCD on the breadboard.

  - Connect the VCC pin to the 5V power rail on the breadboard
  - Connect the GND pin to the ground rail on the breadboard
  - Connect the SDA pin to Analog GPIO 21
  - Connect the SCL pin to Analog GPIO 22

**Step 5:** Ensure all components share a common GND connection, then verify all wiring before connecting the ESP32 board to your computer using the USB cable.

![Components on breadboard](../../../assets/coder_pro/expert/expert_II/4.2.15_Project_105_Joystick_Stepper_Precision_Micro_Positioner/image_1.png)

**Step 6:** After confirming that all connections are correct, connect the USB cable to the ESP32 board and the computer to power the circuit.

_Make sure to connect the Micro USB cable to the ESP32 board._

## PROGRAMMING

**Step 1:** Open your Arduino IDE. See how to set up here: [Getting Started](../../Getting Started/Arduino_IDE_Setup.md).

**Step 2:** Write the complete program implementing the system logic with appropriate pin definitions, setup configuration, and the main control loop.

```cpp
#include <Stepper.h>
#include <Wire.h>
#include <LiquidCrystal_I2C.h>

const int stepsPerRevolution = 2048;
Stepper myStepper(stepsPerRevolution, 8, 10, 9, 11);
LiquidCrystal_I2C lcd(0x27, 16, 2);

const int joyX = 36;
const int joySW = 2;

void setup() {
  myStepper.setSpeed(10);
  lcd.init();
  lcd.backlight();
  pinMode(joySW, INPUT_PULLUP);
  lcd.setCursor(0, 0);
  lcd.print("System Ready");
}

void loop() {
  int xVal = analogRead(joyX);
  if (xVal < 400) {
    myStepper.step(10);
    lcd.setCursor(0, 1);
    lcd.print("Moving CW ");
  } else if (xVal > 600) {
    myStepper.step(-10);
    lcd.setCursor(0, 1);
    lcd.print("Moving CCW");
  } else {
    lcd.setCursor(0, 1);
    lcd.print("Stationary");
  }
}

```

**Step 7:** Save your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section_

**Step 8:** Select the ESP32 board and port _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_.

**Step 9:** Upload your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_

## CONCLUSION

Operating physical input devices instantly modifies actuator behavior and updates visual indicators in real time.
