# 4.1.1. BME280 LCD Environmental Monitor

| **Description** | This project teaches multi-device I2C bus sharing, screen layout formatting, and sensor data presentation. These skills are fundamental to weather station displays, HVAC climate controllers, and portable environmental diagnostic tools. |
|------------------|----------------------------------------------------------------|
| **Use case**     | This project connects to real-world industrial and IoT applications such as: weather stations, climate-control systems, and environmental monitoring devices. It demonstrates how multiple sensors and displays can share one communication bus efficiently. |

## Components (Things You will need)

| ![ESP32 board](../../../assets/components/esp32.PNG) | ![Micro USB cable](../../../assets/components/usbcable.webp) | ![Bread Board](../../../assets/components/breadboard.webp) | ![Jumper Wires](../../../assets/components/jumperwires.webp) | ![BME Sensor](../../../assets/components/bme.jpg) | ![LCD Display](../../../assets/components/lcdscreen.jpg) |
| --- | --- | --- | --- | --- | --- |

## Building the circuit

> **Tip:** Use the [ESP32 Pin Mapping](../../assets/esp32_pin_mapping.png) image to locate the correct GPIO pins on your ESP32 board.


Things Needed:

- ESP32 board = 1

- Micro USB cable = 1

- Breadboard = 1

- Jumper Wires

- BME Sensor

- LCD Display

## Mounting the component on the breadboard and wiring the circuit

**Step 1:** Place the Components

Place the BME280 sensor and I²C LCD display on the breadboard.

**Step 2:** Connect the BME280 Power and Ground

Connect the VCC pin of the BME280 sensor to the 3.3V pin on the ESP32 board using a jumper wire.

Then, connect the GND pin to GND.

**Step 3:** Connect the BME280 I²C Pins

Connect the SDA pin of the BME280 sensor to GPIO 21 (SDA) on the ESP32 board.

Then, connect the SCL pin to GPIO 22 (SCL).

**Step 4:** Connect the I²C LCD Power and Ground

Connect the VCC pin of the I²C LCD display to 5V on the ESP32 board and the GND pin to GND using jumper wires.

**Step 5:** Connect the I²C LCD Pins

Connect the SDA pin of the I²C LCD display to GPIO 21 (SDA) and the SCL pin to GPIO 22 (SCL) on the ESP32 board.

**Step 6:** Perform a Final Check

Check all power, signal, and ground connections against the Circuit Connections table and wiring diagram.

Ensure that all components share a common ground and that the BME280 is connected to 3.3V and the I²C LCD display is connected to 5V before powering the circuit.

![Components on breadboard](../../../assets/coder_pro/expert/BME280_LCD_Environmental_Monitor/circuit_1.png)

_Make sure to connect the Micro USB cable to the ESP32 board._

## PROGRAMMING

**Step 1:** Open your Arduino IDE. See how to set up here: [Getting Started](../../Getting Started/Arduino_IDE_Setup.md).

**Step 2:** Write the complete program implementing the system logic with appropriate pin definitions, setup configuration, and the main control loop.

```cpp
// Project 62: BME280 LCD Environmental Monitor
#include <Wire.h>
#include <Adafruit_BME280.h>
#include <LiquidCrystal_I2C.h>

Adafruit_BME280 bme;
LiquidCrystal_I2C lcd(0x27, 16, 2);

unsigned long lastUpdate = 0;
const long interval = 2000;

void setup() {
  Wire.begin();
  lcd.init();
  lcd.backlight();
  lcd.setCursor(0, 0);
  lcd.print("BME280 Station");
  lcd.setCursor(0, 1);
  lcd.print("Initializing...");
  delay(1500);

  if (!bme.begin(0x76)) {
    lcd.clear();
    lcd.print("BME280 Error!");
    while (1);
  }
  lcd.clear();
}

void loop() {
  if (millis() - lastUpdate >= interval) {
    lastUpdate = millis();
    float temp = bme.readTemperature();
    float hum = bme.readHumidity();
    float pres = bme.readPressure() / 100.0F;

    lcd.setCursor(0, 0);
    lcd.print("T:"); lcd.print(temp, 1); lcd.print("C H:"); lcd.print(hum, 0); lcd.print("%");
    lcd.setCursor(0, 1);
    lcd.print("P:"); lcd.print(pres, 1); lcd.print(" hPa    ");
  }
}

```

**Step 3:** Save your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section_

**Step 4:** Select the ESP32 board and port _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_.

**Step 5:** Upload your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_

## CONCLUSION

The screen shows initialization text for 1.5 seconds, then presents continuous live temperature, humidity, and atmospheric pressure.
