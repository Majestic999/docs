# Project 3.28.1: Smart Weather Station Console

| **Description** | A smart weather station that simulates environmental monitoring by using an LDR module to represent UV light intensity, an ultrasonic sensor to simulate rainfall level, an RGB LED module to indicate weather conditions, a buzzer to warn of storm conditions, and a push button to switch between display modes. |
|------------------|----------------------------------------------------------------|
| **Use case**     |This project can be used in smart weather monitoring systems, environmental sensing, educational STEM projects, IoT weather stations, and embedded applications that require monitoring and displaying environmental conditions. |

## Components (Things You will need)

| ![ESP32 board](../../../assets/components/esp32.PNG) | ![Micro USB cable](../../../assets/components/usbcable.webp) | ![Breadboard](../../../assets/components/breadboard.webp) |![Jumper wire](../../../assets/components/jumperwires.webp) |![Push Button](../../../assets/components/pushbutton.webp) |![Buzzer ](../../../assets/components/buzzer.webp)| ![RGB LED](../../../assets/components/RGB.webp)| ![LDR Module](../../../assets/components/ldr.webp) |![Ultrasonic Sensor ](../../../assets/components/ultrasonic.webp)|
|-------------------------|-------------------------|-------------------------|-------------------------|-------------------------|--------------------------|-------------------------|--------------------------|--------------------------|

## Building the circuit

> **Tip:** Use the [ESP32 Pin Mapping](../../assets/esp32_pin_mapping.png) image to locate the correct GPIO pins on your ESP32 board.


Things Needed:

- ESP32 board = 1

- Micro USB cable = 1

- Push button = 1

- LDR module = 1

- Ultrasonic sensor = 1

- RGB LED module = 1

- Buzzer = 1

- Jumper Wires

## Mounting the component on the breadboard

**Step 1:** Carefully mount the push button, LDR module, ultrasonic sensor, RGB LED module, buzzer on the breadboard.

![Components on breadboard](../../../assets/3.0/Smart_Weather_Station_Console/circuit_1.png)

_**NB:** For complex circuits, plan your component placement to minimize wire crossing and ensure clean connections._

## WIRING THE CIRCUIT

**Step 2:** Connect the 5V pin on the ESP32 board to the positive (+) power rail on the breadboard.

Connect the GND pin on the ESP32 board to the negative (-) power rail on the breadboard.

![Wiring diagram](../../../assets/3.0/Smart_Weather_Station_Console/circuit_2.png)

**Step 3:** Connect the 5V pin on the ESP32 board to the positive (+) power rail on the breadboard.

Connect the GND pin on the ESP32 board to the negative (-) power rail on the breadboard.

![Wiring diagram](../../../assets/3.0/Smart_Weather_Station_Console/circuit_3.png)

**Step 4:** Connecting the Push Button.

Connect one terminal of the push button to Digital GPIO 2.

Connect the opposite terminal to GND.

![Wiring diagram](../../../assets/3.0/Smart_Weather_Station_Console/circuit_4.png)

**Step 5:** Connecting the RGB LED Module.

Connect the Red signal pin to Digital GPIO 4.

Connect the Green signal pin to Digital GPIO 18.

Connect the Blue signal pin to Digital GPIO 19.

Connect the module GND pin to GND.

![Wiring diagram](../../../assets/3.0/Smart_Weather_Station_Console/circuit_5.png)


**Step 6:** Connecting the Buzzer.

Connect the positive (+) pin to Digital GPIO 21.

Connect the negative (-) pin to GND.

![Wiring diagram](../../../assets/3.0/Smart_Weather_Station_Console/circuit_6.png)

**Step 7:** Connecting the Ultrasonic Sensor.

Connect VCC to 5V.

Connect GND to GND.

Connect TRIG to Digital GPIO 22.

Connect ECHO to Digital GPIO 23.

![Wiring diagram](../../../assets/3.0/Smart_Weather_Station_Console/circuit_7.png)

**Step 8:** Connecting the LDR Module.

Connect VCC to 5V.

Connect GND to GND.

Connect AO to Analog GPIO 36.

![Wiring diagram](../../../assets/3.0/Smart_Weather_Station_Console/circuit_8.png)

_Make sure to connect the Micro USB cable to the ESP32 board._

## PROGRAMMING

**Step 1:** Open your Arduino IDE. See how to set up here: [Getting Started](../../Getting Started/Arduino_IDE_Setup.md).

**Step 2:** Write the complete program implementing the system logic with appropriate pin definitions, setup configuration, and the main control loop.

```cpp
// Libraries
// (No external libraries required)

// Pin Definitions
const int buttonPin = 2;

const int redPin = 4;
const int greenPin = 18;
const int bluePin = 19;

const int buzzerPin = 21;

const int trigPin = 22;
const int echoPin = 23;

const int ldrPin = 36;

bool uvMode = true;
bool lastButton = HIGH;

long getDistance()
{
  digitalWrite(trigPin, LOW);
  delayMicroseconds(2);

  digitalWrite(trigPin, HIGH);
  delayMicroseconds(10);

  digitalWrite(trigPin, LOW);

  long duration = pulseIn(echoPin, HIGH);
  return duration * 0.034 / 2;
}

void setRGB(bool r, bool g, bool b)
{
  digitalWrite(redPin, r);
  digitalWrite(greenPin, g);
  digitalWrite(bluePin, b);
}

void setup()
{
  pinMode(buttonPin, INPUT_PULLUP);

  pinMode(redPin, OUTPUT);
  pinMode(greenPin, OUTPUT);
  pinMode(bluePin, OUTPUT);

  pinMode(buzzerPin, OUTPUT);

  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);

  Serial.begin(9600);
}

void loop()
{
  bool button = digitalRead(buttonPin);

  if (lastButton == HIGH && button == LOW)
  {
    uvMode = !uvMode;
    delay(250);
  }

  lastButton = button;

  int lightLevel = analogRead(ldrPin);
  long rainLevel = getDistance();

  if (uvMode)
  {
    Serial.print("UV Level: ");
    Serial.println(lightLevel);

    if (lightLevel > 700)
      setRGB(HIGH, LOW, LOW);        // High UV
    else if (lightLevel > 400)
      setRGB(LOW, HIGH, LOW);        // Moderate UV
    else
      setRGB(LOW, LOW, HIGH);        // Low UV
  }
  else
  {
    Serial.print("Rain Level: ");
    Serial.print(rainLevel);
    Serial.println(" cm");

    if (rainLevel < 10)
    {
      setRGB(HIGH, LOW, LOW);        // Storm
      tone(buzzerPin, 1000);
    }
    else if (rainLevel < 25)
    {
      setRGB(LOW, HIGH, LOW);        // Rain
      noTone(buzzerPin);
    }
    else
    {
      setRGB(LOW, LOW, HIGH);        // Clear
      noTone(buzzerPin);
    }
  }

  delay(200);
}
```

**Step 3:** Save your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section_

**Step 4:** Select the ESP32 board and port _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_.

**Step 5:** Upload your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_


## CONCLUSION

In this project, you learned how to build a smart weather station console using an ESP32 board, an LDR module, an ultrasonic sensor, an RGB LED module, a push button, and a buzzer. The system demonstrates how environmental conditions can be monitored, interpreted, and displayed using both visual and audible indicators.

By completing this project, you strengthened your understanding of analog sensing, ultrasonic distance measurement, mode selection, RGB LED control, threshold-based decision making, digital outputs, and developing intelligent environmental monitoring systems using ESP32 board.

