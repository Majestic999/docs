# 3.1. Smart Greenhouse Controller

| **Description** | In this project, learners will build a Smart Greenhouse Controller that monitors environmental conditions and automatically responds to changes. The system uses a soil moisture sensor to monitor soil conditions, an LDR to measure light intensity, an LCD to display environmental data, and a buzzer to alert the user when plant conditions become unfavorable. This project introduces environmental automation and demonstrates how technology can improve agricultural productivity. |
|------------------|----------------------------------------------------------------|
| **Use case**     | This project demonstrates how multiple sensors can work together to monitor plant health. It reflects real-world applications such as smart farms, greenhouses, and automated agricultural facilities. |

## Components (Things You will need)

| ![ESP32 board](../../assets/components/esp32.PNG) | ![Micro USB cable](../../assets/components/usbcable.webp) | ![Bread Board](../../assets/components/breadboard.webp) | ![Jumper Wires](../../assets/components/jumperwires.webp) | ![LCD Display](../../assets/components/lcdscreen.jpg) | ![Buzzer](../../assets/components/buzzer.webp) | ![Soil Moisture Sensor](../../assets/components/soilmoisture.jpg) | ![LDR Sensor](../../assets/components/ldr.webp) |
| --- | --- | --- | --- | --- | --- | --- | --- |

## Building the circuit

> **Tip:** Use the [ESP32 Pin Mapping](../../assets/esp32_pin_mapping.png) image to locate the correct GPIO pins on your ESP32 board.

Things Needed:

- ESP32 board = 1

- Micro USB cable = 1

- Breadboard = 1

- Jumper Wires

- LDR Module = 1

- I2C LCD Display = 1

- Buzzer = 1

- Soil Moisture Sensor = 1

---

## Mounting the components on the breadboard

**Step 1:** Mount the ESP32 and Components:

- Align the ESP32 board across the center dividing ridge of the breadboard.

- Place the LDR Module, Buzzer, and Soil Moisture Sensor onto the breadboard.

Ensure their pins sit in separate rows.

- The I2C LCD Display connects via jumper wires (it usually doesn't mount directly on the breadboard).

---

## Wiring the circuit

Follow these step-by-step connection details using your jumper wires:

**Step 1:** Create Power and Ground Rails

- Connect the **5V** or **VIN** pin on the ESP32 board to the positive (red) rail on the breadboard.

- Connect a **GND** pin on the ESP32 board to the negative (blue/black) rail on the breadboard.

**Step 2:** Wire the Soil Moisture Sensor

- Connect the **VCC** pin to the positive power rail.

- Connect the **GND** pin to the negative power rail.

- Connect the **AO** (Analog Out) pin to **GPIO 36** on the ESP32 board.

**Step 3:** Wire the LDR Module

- Connect the **Positive (+)** pin to **GPIO 22** on the ESP32 board. (Wait, the original text says "Digital GPIO 22", but the code uses 39 for the light sensor! I will follow the original code which makes more sense for analog read: 39).

- Let me correct this.

Connect the **VCC/Positive** pin of the LDR Module to the positive power rail (Wait, usually LDR modules have VCC, GND, and DO/AO. If it's a simple LDR, it needs a voltage divider. The original text said: `Connect the positive (+) pin to Digital GPIO 22 and the negative (-) pin to GND`. And then code reads from `lightPin = 39`. Let me follow standard 3-pin LDR module wiring: **VCC** to 5V, **GND** to GND, **AO** to **GPIO 39**).

**Revised Step 3:** Wire the LDR Module

- Connect the **VCC (+) ** pin of the LDR Module to the 3.3V pin on the ESP32 board (or 3.3V power rail).

- Connect the **GND (-)** pin of the LDR Module to the negative power rail.

- Connect the **AO** (Analog Out) pin to **GPIO 39** on the ESP32 board.

**Step 4:** Wire the I2C LCD Display

- Connect the **VCC** pin to the positive power rail.

- Connect the **GND** pin to the negative power rail.

- Connect the **SDA** pin to **GPIO 21** on the ESP32 board.

- Connect the **SCL** pin to **GPIO 22** on the ESP32 board.

**Step 5:** Wire the Buzzer

- Connect the **Positive (+)** pin of the Buzzer to **GPIO 23** on the ESP32 board. (Wait, original code used 22, but SCL is also 22! Let's change Buzzer to **GPIO 23** to avoid conflict with the I2C SCL pin).

- Connect the **Negative (-)** pin of the Buzzer to the negative ground rail.

![Components on breadboard](../../assets/coder_pro/advance/Smart_Greenhouse_Controller/circuit_1.png)

*Make sure to connect the Micro USB cable securely to the ESP32 board and your computer.*

---

## PROGRAMMING

**Step 1:** Open your Arduino IDE. See how to set up here: [Getting Started](../Getting Started/Arduino_IDE_Setup.md).

**Step 2:** Write the code in sections as detailed below.

### 1. Library Inclusion and Pin Definitions

```cpp
// Include LCD libraries
#include <Wire.h>
#include <LiquidCrystal_I2C.h>

// Create LCD object
LiquidCrystal_I2C lcd(0x27, 16, 2);

// Sensor and Buzzer pins
const int moisturePin = 36;
const int lightPin = 39;
const int buzzerPin = 23;
```
*Explanation:* We include the `Wire` and `LiquidCrystal_I2C` libraries to control the display. We initialize the LCD object with its I2C address (`0x27`) and dimensions (`16x2`). Pin definitions are set up for the analog sensors and the output buzzer.

---

### 2. Setup Function

```cpp
void setup() {
  // Initialize LCD
  lcd.init();
  lcd.backlight();
  
  // Configure buzzer pin
  pinMode(buzzerPin, OUTPUT);
}
```
*Explanation:* In `setup()`, the LCD is initialized and its backlight turned on so we can see the text. The buzzer pin is configured as an `OUTPUT` so we can send signals to it.

---

### 3. Main Loop Function

```cpp
void loop() {
  // Read sensors
  int moisture = analogRead(moisturePin);
  int light = analogRead(lightPin);
  
  // Display moisture level
  lcd.setCursor(0, 0);
  lcd.print("M:");
  lcd.print(moisture);
  lcd.print("  "); // Clear trailing characters
  
  // Display light level
  lcd.setCursor(8, 0);
  lcd.print("L:");
  lcd.print(light);
  lcd.print("  "); // Clear trailing characters
  
  // Check plant conditions
  if (moisture < 300 || light < 250) {
    // Alert user
    digitalWrite(buzzerPin, HIGH);
    lcd.setCursor(0, 1);
    lcd.print("Check Plants!   ");
  } else {
    // Conditions are good
    digitalWrite(buzzerPin, LOW);
    lcd.setCursor(0, 1);
    lcd.print("Conditions OK   ");
  }
  
  delay(500);
}
```
*Explanation:* 

- The loop continuously reads the analog values from the moisture sensor and the light sensor. 

- It updates the LCD display's first row with the real-time sensor data (`M` for moisture, `L` for light).

- If the moisture drops below 300 or the light falls under 250, the system triggers the buzzer and displays a warning on the second row of the LCD.

- When conditions recover, the buzzer is silenced and the LCD indicates "Conditions OK".

---

## Uploading the code

**Step 3:** Save your code. _See the [Getting Started](../Getting Started/Arduino_IDE_Setup.md) section_

**Step 4:** Select the ESP32 board and port _See the [Getting Started](../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_.

**Step 5:** Upload your code. _See the [Getting Started](../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_

## CONCLUSION

When the program is running, the system continuously monitors the soil moisture and light intensity. If either reading falls below the predefined threshold, the LCD displays a warning message and the buzzer sounds to alert the user to check the plants. When both readings are within the acceptable range, the LCD indicates that conditions are suitable, and the buzzer remains off. This demonstrates how the Smart Greenhouse Controller can automatically monitor environmental conditions and provide timely feedback to support healthy plant growth.
