# Project 3.7.1: Sound-Controlled Desk Lamp

| **Description** | This project creates a desk lamp that can be controlled using claps or loud sounds. The sound sensor toggles the lamp modes, while a potentiometer adjusts the brightness and colour intensity of the RGB LED. |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Use case**    | Hands-free lighting, smart desk lamps, bedroom lighting, interactive electronics.                                                                                                                                 |

## Components (Things You will need)

| ![ESP32 board](../../../assets/components/esp32.PNG) | ![Bread Board](../../../assets/components/breadboard.webp) | ![Micro USB cable](../../../assets/components/usbcable.webp) | ![Sound Sensor](../../../assets/components/soundsensor.webp) | ![RGB LED](../../../assets/components/RGB.webp) | ![Potentiometer](../../../assets/components/potentiometer.png) | ![Jumper wires](../../../assets/components/jumperwires.webp) |
| --------------------------------------------------- | ------------------------------------------------------ | ----------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------ | ------------------------------------------------------ |

## Building the circuit

> **Tip:** Use the [ESP32 Pin Mapping](../../assets/esp32_pin_mapping.png) image to locate the correct GPIO pins on your ESP32 board.


Things Needed:

- ESP32 board = 1

- Micro USB cable = 1

- Breadboard = 1

- Sound Sensor Module = 1

- RGB LED = 1

- Potentiometer = 1

- Jumper Wires

## WIRING THE CIRCUIT

**Step 1:** Connect one jumper wire from the 5V pin on the ESP32 board to the positive rail of the breadboard and connect another jumper wire from the GND pin on the ESP32 board to the negative rail of the breadboard.

![Circuit 1](../../../assets/3.0/Sound_Controlled_Desk/circuit_1.png).

**Step 2:** Place the SOUND SENSOR on the breadboard.

Connect the sound sensor:

- VCC → 5V

- GND → GND

- DO → GPIO 21

![Circuit 2](../../../assets/3.0/Sound_Controlled_Desk/circuit_2.png).

**Step 3:** Place the RGB LED on the breadboard.

Connect the RGB LED:

- Red → GPIO 4

- Green → GPIO 18

- Blue → GPIO 19

![Circuit 3](../../../assets/3.0/Sound_Controlled_Desk/circuit_3.png).

**Step 4:** Place the POTENTIOMETER on the breadboard.

Connect the potentiometer:

- Left pin → 5V

- Right pin → GND

- Middle pin → GPIO 36

![Circuit 4](../../../assets/3.0/Sound_Controlled_Desk/circuit_4.png).

**Step 5:** After completing the wiring, connect the ESP32 board to the computer using the USB cable.

## PROGRAMMING

Below is the ESP32 board code to make the Sound-Controlled Desk Lamp work.

**Step 1:** Open your Arduino IDE. See how to set up here: [Getting Started](../../Getting Started/Arduino_IDE_Setup.md).

**Step 2:** Copy and paste the following code into a new sketch:

```cpp
const int soundPin = 21;
const int redPin = 4;
const int greenPin = 18;
const int bluePin = 19;
const int potPin = 36;

int mode = 0; // 0=Red, 1=Green, 2=Blue, 3=White
bool lastSoundState = HIGH;

void setup() {
  pinMode(soundPin, INPUT);
  pinMode(redPin, OUTPUT);
  pinMode(greenPin, OUTPUT);
  pinMode(bluePin, OUTPUT);

  Serial.begin(9600);
}

void loop() {

  int soundState = digitalRead(soundPin);

  // Detect clap/sound trigger
  if (soundState == LOW && lastSoundState == HIGH) {
    mode++;

    if (mode > 3) {
      mode = 0;
    }

    delay(200);
  }

  lastSoundState = soundState;


  // Read potentiometer value
  int potValue = analogRead(potPin);

  // Convert 0-4095 value to LED brightness 0-255
  int brightness = map(potValue, 0, 4095, 0, 255);


  // Select RGB colour mode

  if (mode == 0) {

    // Red
    analogWrite(redPin, brightness);
    analogWrite(greenPin, 0);
    analogWrite(bluePin, 0);

  } 

  else if (mode == 1) {

    // Green
    analogWrite(redPin, 0);
    analogWrite(greenPin, brightness);
    analogWrite(bluePin, 0);

  } 

  else if (mode == 2) {

    // Blue
    analogWrite(redPin, 0);
    analogWrite(greenPin, 0);
    analogWrite(bluePin, brightness);

  } 

  else if (mode == 3) {

    // White
    analogWrite(redPin, brightness);
    analogWrite(greenPin, brightness);
    analogWrite(bluePin, brightness);

  }

  Serial.print("Mode: ");
  Serial.println(mode);

  Serial.print("Brightness: ");
  Serial.println(brightness);

  delay(50);
}
```

**Step 3:** Save your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section_.

**Step 4:** Select ESP32 board from Tools → Board.

**Step 5:** Select the correct COM port from Tools → Port.

**Step 6:** Click Upload.

## OBSERVATION

- Each clap changes the lamp mode.

- The potentiometer adjusts brightness.

- The RGB LED changes between multiple colour presets.

## CONCLUSION

This project demonstrates sound sensing, PWM control, RGB colour mixing, and analog input processing.

