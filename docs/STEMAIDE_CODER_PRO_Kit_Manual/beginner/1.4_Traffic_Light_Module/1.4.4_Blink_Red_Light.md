# Project 1.4.4: Traffic Light Red Only Blink

| **Description** | This is where you get to know how to make the red LED blink.                                        |
| --------------- | --------------------------------------------------------------------------------------------------- |
| **Use case**    | Programming the red light on the traffic to blink which could be used to signal danger on the road. |

## Components (Things You will need)

| ![Traffic light module ](../../../assets/components/trafficmodule.webp) | ![ESP32 board](../../../assets/components/esp32.PNG) | ![Micro USB cable](../../../assets/components/usbcable.webp) | ![Breadboard](../../../assets/components/breadboard.webp) | ![Jumper Wires](../../../assets/components/jumperwires.webp) |
| ------------------------------------------------------------------- | --------------------------------------------------- | ----------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------ |

## Building the circuit

> **Tip:** Use the [ESP32 Pin Mapping](../../../assets/esp32_pin_mapping.png) image to locate the correct GPIO pins on your ESP32 board.

Things Needed:

- ESP32 board = 1

- Micro USB cable = 1

- Traffic light module = 1

- Jumper Wires

- Breadboard = 1

---

## Mounting the components on the breadboard

**Step 1:** Mount the ESP32 board and Traffic Light Module:

- Align the ESP32 board across the center dividing ridge of the breadboard so that its left pins sit in Column E and its right pins sit in Column F.

- Place the Traffic Light Module on the breadboard. It has 4 pins: `G` (Green), `Y` (Yellow), `R` (Red), and `GND`.

Insert these 4 pins into separate adjacent rows (e.g., Rows 25 to 28, Column A).
  - `G` pin in **Row 25, Column A**
  - `Y` pin in **Row 26, Column A**
  - `R` pin in **Row 27, Column A**
  - `GND` pin in **Row 28, Column A**

![Traffic Light fixed on breadboard](../../../assets/coder_pro/beginner/Traffic_Light_Module/Traffic_Light_Red_On/circuit_3.png)

---

## Wiring the circuit

Follow these step-by-step connection details using your jumper wires:

**Step 1:** Wire the Red Light to the ESP32

- Connect a jumper wire from **Row 27, Column B** (R pin) to **GPIO 18** on the ESP32 board.

**Step 2:** Wire the common Ground

- Connect a jumper wire from **Row 28, Column B** (GND pin) to a **GND pin** on the ESP32 board.

*Make sure to connect the Micro USB cable securely to the ESP32 board and your computer.*

---

## PROGRAMMING

**Step 1:** Open your Arduino IDE. See how to set up here: [Getting Started](../../Getting Started/Arduino_IDE_Setup.md).

**Step 2:** Write the code in sections as detailed below.

### 1. Variable Declarations

```cpp
const int red = 18;
```
*Explanation:* We declare a constant integer variable `red` and set it to 18, mapping to GPIO 18 where the Red pin is connected.

---

### 2. Setup Function

```cpp
void setup() {
  pinMode(red, OUTPUT);
}
```
*Explanation:* The `setup()` function runs once. It configures the `red` pin as an `OUTPUT` so the ESP32 can send electrical signals to turn it on and off.

---

### 3. Loop Function

```cpp
void loop() {
  digitalWrite(red, HIGH);
  delay(400);
  digitalWrite(red, LOW);
  delay(400);
}
```
*Explanation:* The `loop()` continuously runs. 
First, `digitalWrite(red, HIGH)` turns the red LED ON. 
Next, `delay(400)` pauses the program for 400 milliseconds.

Then, `digitalWrite(red, LOW)` turns the LED OFF. 
Finally, another `delay(400)` pauses the program for another 400 milliseconds before repeating the cycle. This creates a rapid blinking effect.

---

## Uploading the code

**Step 3:** Save your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section_

**Step 4:** Select the ESP32 board and port _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_.

**Step 5:** Upload your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_

## OBSERVATION

When the circuit is functioning, observe the Red LED of the traffic light blinking as expected. This indicates that the LED is receiving the signal correctly from the ESP32 board and is operating as intended.

## CONCLUSION

The project focusing on creating a blinking red light within a simulated traffic light system offers an engaging introduction to controlled visual signaling. By programming the red LED to blink, participants learn about timing control, code logic, and the significance of rhythmic signaling. This endeavor serves as a foundational step in electronics exploration, showcasing the concept of dynamic visual cues and sparking curiosity about practical applications, such as traffic safety and automation.
