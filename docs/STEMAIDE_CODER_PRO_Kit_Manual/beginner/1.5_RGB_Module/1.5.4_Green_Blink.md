# Project 1.5.4: GREEN BLINK

| **Description** | A blinking green LED means the system is working normally and everything is okay. |
|------------------|----------------------------------------------------------------|
| **Use case**     |A blinking green LED can be used in real-life systems to indicate that a device is active and working properly, often showing normal operation or that a process is currently running, such as a system booting up, data processing, or a machine functioning without errors. |

## Components (Things You will need)

| ![ESP32 board](../../../assets/components/esp32.PNG) | ![Micro USB cable](../../../assets/components/usbcable.webp) | ![Breadboard](../../../assets/components/breadboard.webp) |![Jumper Wires](../../../assets/components/jumperwires.webp)|![RGB](../../../assets/components/RGB.webp)|
|-------------------------|-------------------------|-------------------------|-------------------------|-------------------------|

## Building the circuit

> **Tip:** Use the [ESP32 Pin Mapping](../../../assets/esp32_pin_mapping.png) image to locate the correct GPIO pins on your ESP32 board.

Things Needed:

- ESP32 board = 1

- Micro USB cable = 1

- RGB Module = 1

- Jumper wires

- Breadboard = 1

---

## Mounting the components on the breadboard

**Step 1:** Mount the ESP32 and RGB module onto the breadboard:

- Align the ESP32 board across the center dividing ridge of the breadboard so that its left pins sit in Column E and its right pins sit in Column F.

- Locate the 4 pins on the RGB module (typically R, GND, G, B).

Insert the RGB module into the breadboard so each pin is in its own row (e.g., Rows 20 to 23, Column A):
  - `R` (Red) pin in **Row 20, Column A**
  - `GND` (-) pin in **Row 21, Column A**
  - `G` (Green) pin in **Row 22, Column A**
  - `B` (Blue) pin in **Row 23, Column A**

![LED fixed on breadboard](../../../assets/coder_pro/beginner/RGB_Module/circuit_1.png)

---

## Wiring the circuit

Follow these step-by-step connection details using your jumper wires:

**Step 1:** Wire the RGB module to the ESP32 board:

- Connect a Green jumper wire from **Row 22, Column B** (the G pin of the RGB module) to **GPIO 19** on the ESP32 board.

- Connect a White (or Black) jumper wire from **Row 21, Column B** (the GND pin of the RGB module) to a **GND pin** on the ESP32 board.

![LED fixed on breadboard](../../../assets/coder_pro/beginner/RGB_Module/circuit_2.png)

*Make sure to connect the Micro USB cable securely to the ESP32 board and your computer.*

---

## PROGRAMMING

**Step 1:** Open your Arduino IDE. See how to set up here: [Getting Started](../../Getting Started/Arduino_IDE_Setup.md).

**Step 2:** Write the code in sections as detailed below.

### 1. Variable Declarations

```cpp
const int green = 19;
```
*Explanation:* We declare a constant integer variable named `green` and assign it the value `19`. This tells the ESP32 that the green pin of our RGB module is connected to GPIO 19.

---

### 2. Setup Function

```cpp
void setup() {
  pinMode(green, OUTPUT);
}
```
*Explanation:* The `setup()` function runs once. It configures the `green` pin as an `OUTPUT` so the ESP32 can send electrical signals (voltage) out of this pin to power the LED.

---

### 3. Loop Function

```cpp
void loop() {
  digitalWrite(green, HIGH);
  delay(400);
  digitalWrite(green, LOW);
  delay(400);
}
```
*Explanation:* The `loop()` continuously runs. 
First, `digitalWrite(green, HIGH)` turns the green light ON. 
Next, `delay(400)` pauses the program for 400 milliseconds.

Then, `digitalWrite(green, LOW)` turns the light OFF. 
Finally, another `delay(400)` pauses the program for another 400 milliseconds before repeating the cycle. This creates a rapid blinking effect.

---

## Uploading the code

**Step 3:** Save your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section_

**Step 4:** Select the ESP32 board and port _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_.

**Step 5:** Upload your code. _See the [Getting Started](../../Getting Started/Arduino_IDE_Setup.md) section:Selecting ESP32 board Type and Uploading your code_

## CONCLUSION

The project centered on creating a blinking green light within an RGB configuration offers a captivating insight into color manipulation and electronic control. By programming the green LED component to blink, participants gain understanding in timing control, code logic, and the dynamic visual effects of a single color. This endeavor marks an important milestone in exploring RGB color variation, showcasing the rhythmic potential of individual color components, and igniting curiosity in practical applications such as animated lighting displays and creative visual presentations.
